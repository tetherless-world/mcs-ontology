version: 2
jobs:
  hygiene_tests:
    working_directory: /git-mcs-ontology
    docker:
      - image: docker/compose:latest
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run ontology hygiene tests
          command: |
            sh run-hygiene.sh
workflows:
  version: 2
  build_and_test:
    jobs:
      - hygiene_tests